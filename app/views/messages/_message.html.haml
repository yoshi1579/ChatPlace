.sent_msg
    %p.messages{"data-id" => "#{message.id}" }
        = message.content
    - if message.image?
        .sent_msg__image
            = image_tag message.image.to_s, size: "100x100"
    %span.time_date 
        = message.user.name 
        = message.created_at